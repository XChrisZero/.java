package enums;

// enum para definição de modelo especificado
// por convenção todos obj dentro do enum são em maiusculo

public enum Modelo
{
    SEDAN,
    HATCH,
    SUV,
    UTILITARIO
}
